# all properties in application.yml or application.properties will be regard as common propeties.
# And they will work on all applications those get propeties from config server.
spring:
  profiles: test
  
jdbc: 
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3307/test
    username: elftest
    password: elftest

redis:
  database: 0  # schema
  host: 127.0.0.1
  port: 6379
  password: ZZdfawt4e  # Login password of the redis server.
  timeout: 100  
  pool.max-active: 8
  pool.max-idle: 8
  pool.max-wait: 10
  pool.min-idle: 0
  transactionSupport: false
# sentinel.master:  # Name of Redis server.
# sentinel.nodes:  # Comma-separated list of host:port pairs.  

#freemarker:
#  enabled: true
#  template-loader-path: /WEB-INF/view  # also you can set like - classpath:/templates
#  suffix: .ftl